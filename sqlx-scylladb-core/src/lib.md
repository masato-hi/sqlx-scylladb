# sqlx-scylladb-core

Internal implementation crate for `sqlx-scylladb`.

This crate contains the ScyllaDB-specific implementations of the sqlx database, connection, query, type, migration, and testing interfaces. Applications should normally depend on `sqlx-scylladb` instead of using this crate directly.
